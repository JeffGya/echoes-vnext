# Echoes vNext — Integration Map

> **Active story tracker** — replaces `docs/v2-migration-map.md` for all work after V2-MIG-001 (Alignment wave complete 2026-04-06).
>
> **Source of truth:** `docs/Echoes vNext Working GDD.md`
> **Backlog:** Notion DB `d3dc9cb4-21e9-44fc-9229-238474907ed6`
> **Conventions:** `CONVENTIONS.md`

---

## How to Read This Map

| Column | Meaning |
|--------|---------|
| Story | Story ID + name from backlog |
| Wave | Alignment / Foundation / Expansion / Full Game |
| Status | Pending / In Progress / Done |
| Ships | Key files changed or created |
| Notes | Deferred decisions, dependencies, design exceptions |

---

## Foundation Wave — Sanctum Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-SANCTUM-001** — Sanctum Pulse + Emotion Recovery | Foundation | Done | `EmotionRecoveryService.gd`, `FlowRuntime.gd` hooks, `SanctumScreen` active-effects panel | Bank/Continue settlement recovers morale at 1/min toward base and fear at 0.5/min toward zero. Combat fear persists into the roster; the consumed `last_emotion_settle_unix` window is saved even when rounding yields no delta. Active-effects chips and VOW-002 recovery modifiers remain wired. |
| **V2-SANCTUM-002** — Institutions, Passive Recovery & Sanctum Visualization | Foundation | Done | `SanctumLayoutService.gd` (extended), `SanctumBuildingLayer.gd` (new), `SanctumOccupantLayer.gd` (refocused), `SanctumPlacementLayer.gd` (new), `SanctumSpatialRenderer.gd/.tscn`, `SanctumShell.gd`, `SanctumScreen.gd/.tscn`, `InstitutionService.gd`, `FlowRuntime.gd`, `SaveSchema.gd`, `balance.json`, `tests/SanctumLayoutTests.gd` | Three-layer spatial architecture remains: buildings z=1, Echo occupants z=2, placement z=3. Echo occupants now expose `emotional_status` (never `morale_tier`) and use the shared `EmotionPresentation`/Living Tree theme mapping for token fill. Passive Hearth/Training effects still apply to the full roster at `economy.settle_time`. |

---

## Foundation Wave — Economy Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-ECONOMY-001** — Ase Flame + Ekwan baseline | Foundation | Done | `EconomyService`, `SaveSchema`, `FlowRuntime` bank timer | Ase Flame awakening, offline cap, Ekwan stub added |

---

## Foundation Wave — Progression Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-MIG-001 / V2-MIG-002** — Save bridge + repair | Alignment | Done | `SaveSchema.gd`, `SaveService.gd` | V2 save root keys added additively alongside V1 aliases |
| **V2-PROG-001** — Progression language rename | Alignment | Done | `FlowSanctumState`, snapshots | Standing/Step/Storyweight labels canonical |
| **V2-PROG-002** — Calling seam unification | Alignment | Done | `calling-reference.md`, `EchoFactory` | 6 callings, Twi names documented as target |
| **V2-PROG-003** — Vector expansion (4 → 10 virtue domains) | Alignment | Done | `VectorService.gd`, `balance.json`, `SaveService.gd` | 10 vector keys canonical; backfill repair on load |
| **V2-PROG-004** — Calling V1 → V2 ID migration | Alignment | Done | `CallingService.gd`, `SaveService.gd`, `balance.json` | V2 calling IDs active; V1 IDs migrated on load |
| **V2-PROG-006** — Maturity Expression | Foundation | Done | `MaturityExpressionService.gd` | `expression_band` → nascent/forming/grounded/whole; `presence_strength` 0.1–1.0 |
| **V2-PROG-007** — Calling lattice: Standing-6 + Standing-9 data + adjacency ring | Foundation | Done | `balance.json` (12 S6 expressions, 24 S9 culminations, adjacency ring), `CallingService.gd` (4 new static functions), `ConfigService.gd` (config integrity guard on load), `CallingTests.gd` (11 new tests, 34 total), `calling-reference.md` | Standing-6/9 names from GDD §11 now in config. Adjacency data-driven. `validate_config_integrity()` called at balance load. Standing-9 `twi_provisional` flag for names needing cultural validation (14 of 24). Vector/archetype fit computation at S6 deferred to V2-PROG-009 — noted in that story's Notion page. |
| **V2-PROG-008** — Calling lattice service layer (S6/S9 query functions) | Foundation | Done | `CallingService.gd` (`get_standing_6_options`, `compute_standing_6_pool`, `get_standing_9_options`, `validate_count_integrity`), `ConfigService.gd` (count integrity guard wired at balance load), `CallingTests.gd` (9 new tests, 43 total), `calling-reference.md` (CallingService API table) | S6 pool = own 2 + adj 2 + adj 2 = 6 total. Anchor: `echo["calling"]` → fallback `echo["calling_origin"]` → `[]` for unresolvable. Raw pass-through — PROG-009 owns eligibility gating and UI projection. `validate_count_integrity` runs alongside `validate_config_integrity` at balance load. V2-PROG-009 calls `compute_standing_6_pool` (selection panel) and `get_standing_9_options` (culmination previews). |
| **V2-PROG-009** — Skills Tab: Unlock Loop + Party Toggle Fix | Foundation | Done | `balance.json` (14 skill defs with `type`/`description`/`unlock_conditions`, 6 calling stubs, `calling_constellation` block), `SkillDefinition.gd` (9 required fields), `SaveService.gd` (`unlocked_skills:[]` additive repair), `FlowRuntime.gd` (party toggle fix for flow.sanctum; `sanctum.unlock_skill` handler), `FlowSanctumState.gd` (enriched skill entries with constellation data), `SanctumScreen.tscn`+`.gd` (Constellation Web UI: calling node, S3 nodes, ghost S6/S9, detail strip, expand/collapse), `ConstellationLines.gd` (new, orbital rings + connecting lines via `_draw()`), `SkillUnlockTests.gd` (8 new tests) | Skills tab shows Constellation Web for called echoes; empty state for uncalled. S3 nodes interactable (40 Ase to unlock). Ghost S6/S9 nodes visible, non-interactive (MOUSE_FILTER_IGNORE). `unlocked_skills:[]` per-echo save field. Party toggle now works from flow.sanctum without navigating to echo_party. `_build_skill_entries_for_echo` now accepts `ase_balance` and returns `is_unlocked`, `ase_cost`, `can_afford`, `calling_confirmed`, `constellation_angle`, `constellation_radius`, `tier`. |
| **V2-PROG-010** — Maturity Expression as Live Simulation Factor | Foundation | Done | `balance.json` (8 new config blocks in `data.maturity_expression`: `rank_strength_scale`, `refusal_thresholds_by_band`, `identity_weight_scale`, `presence_dampen_scale`, `fear_self_recovery`, `sanctum_fear_recovery_bonus`, `directive_band_mul`, `rank_benefits_config`), `MaturityExpressionService.gd`, `ActorStateMachine.gd`, `BehaviorArbiter.gd`, `EmotionRecoveryService.gd`, `FlowRuntime.gd`, `FlowSanctumState.gd`, `FlowEncounterState.gd`, `SanctumScreen.tscn`+`.gd`, `EchoRankBenefitGlyph.tscn`+`.gd`, `MaturityExpressionTests.gd` | Hidden maturity-expression remains GDD-aligned. Whole-band leadership calming and morale/fear influence, previously deferred, shipped in the game-wide emotion/leadership integration pass below. Other prior deferrals remain separate stories. |
| **V2-PROG-012** — Autonomy Seam: Hidden Expression Outputs + Divergence Detection | Foundation | Done | `data/balance.json` (`data.maturity_expression.autonomy_outputs` — Judgment/Presence/Composure/Legibility weight blocks; `identity_weight_scale` + `interpretation_swing_max` budget pair; `composure_dampen_scale` renamed from `presence_dampen_scale`; `leadership_presence_scaling`; `directive_interpretation_mul` (replaces `directive_band_mul`); `divergence` block (`min_contest_ratio` 0.28, `decision_scale_epsilon`, `divergence_ignored_directive_actions`, `composure_noise_gate`/`composure_contradiction_gain`, `legibility_specificity_bands`); `calling_behavior.<calling>.absolute_fear_offset`; `data.contact.vector_virtue_composition`/`virtue_vector_key`/`calling_to_virtue_primary` (consolidated from 4 divergent tables); `data.contact.storyweight_speak_partial_step`), `core/actors/MaturityExpressionService.gd`, `core/actors/behaviors/BehaviorArbiter.gd`, `core/actors/DivergenceDetector.gd` (new), `core/actors/ActorStateMachine.gd`, `core/combat/LeadershipEmotionService.gd`, `core/config/IdentityIntegrity.gd` (new), `core/config/ConfigService.gd`, `core/directives/DirectiveService.gd` (`validate_config_integrity()`), `core/realms/ConversationService.gd`, `core/runtime/FlowRuntime.gd`, `core/onboarding/KeeperIntroService.gd`, `core/onboarding/OnboardingService.gd`, `core/progression/WeavingRiteService.gd`, `core/sanctum/RecruitmentService.gd`, `core/state/flow/states/venture/FlowEncounterState.gd`, `data/shouts/combat_expressions.json` (`combat_divergence` bark), `docs/calling-reference.md`, `ui/AppRoot.gd`, `ui/screens/combat/CombatBoardScreen.gd`; new tests `BehaviorArbiterTests.gd`, `CombatDivergenceBarkTests.gd`, `ConversationRepairTests.gd`, `DirectiveConfigTests.gd`, `DivergenceDetectorTests.gd`, `IdentityIntegrityTests.gd`, `LeadershipEmotionTests.gd`; extended `MaturityExpressionTests.gd`, `MovementArbitrationTests.gd`, `RecruitmentServiceTests.gd`, `WeavingRiteTests.gd` | Four named hidden outputs (Judgment/Presence/Composure/Legibility, GDD §11.5) derived per-activation by `MaturityExpressionService.derive_expression()` — **never persisted**. **Five defects fixed:** DEFECT 0 — seven `balance.json` tuning keys the arbiter could never read, silently falling through to `_DEFAULTS`; DEFECT 1 — `presence_strength` computed/threaded/never read, now grades `LeadershipEmotionService` influence strength and radius; DEFECT 2 — identity weighting and the directive multiplier were two perfectly-correlated axes double-counting a ~2.8× swing, now one continuous axis keyed on Judgment (`interpretation_width`) with an `interpretation_swing_max` budget enforced by a config-integrity test; DEFECT 3 — `presence_dampen_scale` was scaled by `rank_strength` not presence, renamed `composure_dampen_scale`, now driven by Composure (reads fear twice: structural `fear_base` as a multiplicative ceiling, situational spike as a subtractive term); Absolute Fear Rule override→offset — per-calling `absolute_fear_threshold` used to *replace* the band table (inert for 6 of 7 callings), now `absolute_fear_offset` composes with it. **Live effect:** divergence detection — `contest_ratio = directive_pull / decision_scale` (a proportion so one threshold generalises across directives), `min_contest_ratio: 0.28`, emits `actor.divergence` (info) and a band-tiered `combat_divergence` bark whose specificity *is* Legibility; gated to `faction == "echo"`. **Conversation repairs folded in:** NPC opening lines were unconditionally overwritten with `""` by an unauthored `burden_variants` lookup (every NPC opened with "…") — now guarded to only overwrite on a non-empty lookup; conversation Storyweight was `int(0.2)` → `0`, never awarded — retuned to 1/winning turn with the `xp_total` clobber fixed and a warn-on-round-to-zero guard (same hardening applied to `KeeperIntroService.apply_first_weave`). **Virtue tables consolidated:** `vector_virtue_composition` (semantic) + `virtue_vector_key` (bijective keying — a composition-faithful bijection is impossible because empathy and forgiveness occur only in `mediator`) + `calling_to_virtue_primary`, guarded by `IdentityIntegrity.validate()`. **Directive robustness:** divergence coverage enumerates `DirectiveService.get_registry()` at runtime (directive-agnostic), and `DirectiveService.validate_config_integrity()` rejects degenerate `intent_weights` at load. **Boundary (locked, see CONVENTIONS.md):** the divergence contract's vocabulary is deliberately not the five Keeper-guidance responses — `V2-COMBAT-003` resolves Align/Interpret/Hesitate/Object/Refuse from these inputs; `V2-VOICE-002` aggregates per-Echo outputs into a party-level read. `directive_bonus` must stay a flat additive term outside the fear/calling bracket in `BehaviorArbiter._score()` — that is what makes `self_score = score − directive_bonus` exact; "tidying" it inward would break divergence detection. **Measured findings (see `ANSWERS.md`):** refusal is unreachable in natural play — 0 refusals in 439 measured rounds across all bands/fresh/veteran Echoes/tripled enemy counts, fear peaks ~27 against thresholds of 60–90 because recovery outpaces accumulation; the Phase 7 band+offset fix is correct but cannot be observed until the fear economy is rebalanced. Divergence is effectively single-directive today — under `seek_signs`, only 4 of 535 Echo turns showed any contest (a directive-content property, not a detector defect — its resolved preference already matches unprompted Echo behavior). **All values marked PROPOSED DEFAULT in `data/balance.json` await playtest ratification** — the `autonomy_outputs` weight blocks, `identity_weight_scale`/`interpretation_swing_max`, `min_contest_ratio` (0.28), `leadership_presence_scaling`, `calling_behavior.*.absolute_fear_offset`, `storyweight_speak_partial_step` (1), and the `virtue_vector_key`/`calling_to_virtue_primary` tables. **Test baseline correction:** the story plan's assumed 882-test baseline (last accurate at V2-STAGE-004 Phase 4, row above) was stale — the real baseline at this branch's start was **1309**; the suite is now **1378** (1378 total, 1378 passed, 0 failed, 0 SCRIPT ERRORs). **Deferred:** items → V2-ITEM-002 and enemy pressure roles → V2-COMBAT-002 deferrals from earlier stories are unaffected; this story adds no new deferrals beyond the five-response mapping (V2-COMBAT-003) and party aggregation (V2-VOICE-002) noted above. |

---

## Foundation Wave — Emotion + Leadership Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **Game-wide Emotion + Whole-band Leadership integration** | Foundation | Done | `EmotionService.gd`, `LeadershipEmotionService.gd`, `ActorStateMachine.gd`, `FlowRuntime.gd`, `FlowEncounterState.gd`, `SanctumLayoutService.gd`, `EmotionPresentation.gd`, Living Tree theme, emotion UI surfaces, `LeadershipEmotionTests.gd` | Ten-tier player-facing ladder: radiant → whole → grounded → uncertain → hesitant → burdened → pressed → strained → fraying → hollow. `emotional_status` is the only player-facing feeling field; hidden morale tiers stay simulation-only. Combat projection strips raw/legacy emotion and uses alive/dead/guarding operational status. Whole-band configured effects cover direct morale/fear recovery, rally/kill momentum, fear contagion reduction/blocking, morale-loss reduction/forecast; strongest reductions do not multiply, while direct recovery from separate leaders stacks. Combat emotion persists through Continue and stage-completion routing. Shared presentation comes from `EmotionPresentation` + `LivingTreeSystem.tres`. **Suite: 736 tests, including 11 LeadershipEmotionTests.** |

---

## Foundation Wave — Vow Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **VOW-001** — Vow pledge/break | Foundation | Done | `VowService.gd`, `VowScreen.gd` | 4 vows, tier system, FlowRuntime pledge/break handlers |
| **V2-VOW-002** — Vow compliance + active effects | Foundation | Done | `SanctumScreen` active-effects panel, `FlowRuntime` vow_stats | Compliance count, morale/fear hit on break, effect chips |

---

## Foundation Wave — Weave + Thread Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-WEAVE-001** — Thread reserve | Foundation | Done | `ThreadService.gd`, `FlowSanctumState` | Thread reserve display, cap config |
| **V2-WEAVE-002** — Weaving Rite | Foundation | Done | `WeavingRiteService.gd`, `WeavingRiteScreen` | Accept/Reject/Defer outcomes. Partial Integrate + Distort deferred to Expansion. |

---

## Foundation Wave — Realm + Stage Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-STAGE-001** — Stage Exploration | Foundation | Done | `StageExploreModel.gd`, `FlowStageExploreState.gd` | Situation model, explore turns, intel persistence |
| **V2-STAGE-002** — Objective Taxonomy + Stage Completion Gate | Foundation | Done | `ObjectiveModel.gd` (6 types + completed/required), `SituationModel.gd` (objective_index), `StageModel.gd` (4 summary types), `RealmGenerator.gd` (config-driven pool + situation-objective binding), `RealmService.gd`, `FlowContext.gd` (active_encounter_objective_index), `FlowRuntime.gd` (objective completion + ignore_situation + emotion effects + fear return), `FlowEncounterState.gd` (objective-type resolution + objectives_remaining routing), `FlowStageExploreState.gd` (completion gate + calling bonuses + party return), `FlowStageState.gd` (reveal_hint/completed/required), `SaveService.gd` (additive repair), `balance.json` (data.stages), `realms.json` (objective_pool), `tests/StageObjectiveTests.gd` (12 tests) | Objectives and encounters are now distinct: `objective_index` binds situations to `stage.objectives[]`. Stage advance blocked until all required objectives complete. Six objective types: combat + shrine fully wired; recover/protect/endure/pursue model-complete + save-safe (V2-STAGE-004 for runtime). Calling-action bonuses (ranger/okofor/aduro/cautious_advance). Non-combat emotion effects config-driven. Party fear return request. `stage.ignore_situation`. Min 2 objectives enforced. **Deferred → V2-STAGE-003:** NPC contacts, `role` field (comment stub in SituationModel). **Deferred → V2-STAGE-004:** Non-combat encounter resolution; distinct combat field layouts. **Deferred → V2-COMBAT-002:** Enemy pressure roles. |
| **V2-STAGE-003** — NPC Contact Conversation System | Foundation | Done | `ContactModel.gd` (new), `ConversationService.gd` (new), `FlowStageExploreState.gd` (contact snapshot fields + actions), `FlowRuntime.gd` (stage.consult_echoes / stage.speak_response / stage.disengage_contact handlers), `RealmGenerator.gd` (contact_cfg param + RNG namespace), `balance.json` (contact_responses block), `data/conversations/contact_responses.json` (new), `SaveService.gd` (additive repair), `tests/ContactModelTests.gd` (12 tests) | 5 contact roles (witness/guide/charge/claimant/temporary_ally), 6 dispositions. Two-factor resonance scoring (virtue wheel + emotional fit). Per-turn echo consultancy (≤3 echoes when party > 3). Outcome effects wired per role (morale/fear/ase/intel/objective/continuity). Claimant failure → hostile marker (V2-STAGE-004 wires combat). **Deferred → V2-STAGE-004:** Hostile-marker → real combat; distinct field layouts for 4 non-NPC objective types. |
| **V2-STAGE-004 Phase 1** — Unified Situation Resolution + Unified Resolve Surface | Foundation | Done | `core/realms/SituationResolutionService.gd` (new — pure-static router + resolver), `SituationModel.gd` (4 new types: TYPE_OMEN/OBSTACLE/RITUAL/STRUCTURE, appended indices 8–11), `FlowStageExploreState.gd` (_handle_stage_engage_situation rewired; `_start_contact_conversation` extracted; `stage.resolve_situation_choice` handler), `FlowEncounterState.gd` (`build_final_snapshot` enriched: surface/verdict/summary_line/direction/tag), `FlowRuntime.gd` (`_build_situation_resolve_snapshot`; encounter_approach routing), `ResolveScreen.gd` (unified 4-zone parametric surface: Banner + Summary + Echo stage + Effects rail), `ResolveScreen.tscn` (DimBackdrop full-rect), `EmotionEntryItem.tscn` (token + %DirectionCueLabel + %KoTagLabel), `ui/components/EffectChip.gd` (new), `ui/components/EffectChip.tscn` (new), `RealmShell.gd` (`_show_resolve_overlay()`; flow.resolve rendered as dim modal overlay), `SaveService.gd` (additive: explore_map.loot_results, stage_context.encounter_approach, per-objective params), `balance.json` (data.stages.situation_resolution block + omen/obstacle/ritual/structure in situation_emotion_effects), `tests/SituationResolutionServiceTests.gd` (17 tests), `tests/UnifiedResolveTests.gd` (13 tests) | **610 total tests, 610 passed.** `SituationResolutionService.route()` decides async (→ ENCOUNTER) vs in_explore (single-outcome or choice overlay). The old `_stub_situation_result` + mutate-then-undo blocks are deleted. `stage.resolve_situation_choice { situation_id, choice_id }` handles obstacle/structure choice paths. `flow.resolve` snapshot extended additively with: `surface`, `verdict` (carried/passed/good/partial/missed), `summary_line`, `emotion_summary[].direction` (lift/ease/steady/fall) + `.tag` (ko/refused). `effects[]` rail ({kind: item/intel/continuity/objective/storyweight, label, value, tone}). RealmShell renders `flow.resolve` as a dim modal overlay over the active venture screen (not a screen swap). **NEW RNG namespace:** `stage.resolution.<sit_id>` (append-only). **Additive save keys:** `explore_map.loot_results []`, `stage_context.encounter_approach {}`, per-objective `params {}`. **Deferrals → P3:** 4 distinct combat objective scenarios + objective-status chips; **V2-ITEM-002:** loot_results seam; **V2-COMBAT-002:** enemy pressure roles; `flow.resolve` state deletion → tracked tech-debt (CONVENTIONS.md note). The 2 previously-failing `party_toggle` tests fixed (additive save-repair + test isolation). |
| **V2-STAGE-004 Phase 2 + 2.5** — Movement-Cost Traversal, Irregular Per-Realm Maps, Data-Driven Directives, Intel-Driven Fog-of-War | Foundation | Done | `core/realms/StageTerrain.gd` (new — deterministic irregular terrain: per-realm signature → plateaus/bridges/stragglers, irregular blobs via seeded border erosion; `walkable_set`/`bfs_distance_field`/`next_step`/`cells_within_radius`/`nearest_unexplored`), `RealmGenerator.gd` (terrain gen + walkable situation placement + virtue→signature resolution), `StageExploreModel.gd` (terrain field), `RealmService.gd` (passes realm cfg for signature), `FlowRuntime.gd` (movement-cost traversal, 4-tier frontier `_find_explore_target`, tile-tied discovery `_reveal_explored_situations`, `_handle_stage_advance_turn`/`_handle_stage_return_home` rewrites, `escape_bonus`/`intel_retention`, pass→`passed` skip), `DirectiveService.gd` (`load_from_config` + const fallback), `FlowStageExploreState.gd` (fog projection + `explored_cells`/`passed` + durable reset + `traveled_path`), `FlowStageState.gd` (overview briefing + `explored_cells`, no markers), `balance.json` (`data.directives` exploration-style + `data.stages.map_shape.by_virtue` [10 signatures] + `situation_category`), `realms.json` (`terrain_signature` override hook), `SaveService.gd` (additive: terrain/explored_cells/in_transit/target_situation_id/last_traveled_path), `StageExploreScreen.gd`+`.tscn` (terrain + 3-state tile-fog via FogLayer; walkable-path chained tween; situation-layer transform sync), `AppRoot.gd` (test registration), `tests/StageTerrainTests.gd`/`TraversalModelTests.gd`/`DirectiveConfigTests.gd` (new) | **663 total tests, 662 passed (1 pre-existing flaky `party_toggle`).** Each of the 10 Realms has a **virtue-keyed landscape signature** (`data.stages.map_shape.by_virtue`) — stages vary deterministically within a realm; **future per-realm visual assets key off realm.id/virtue + the signature `relief` descriptor — do not lose this seam.** Party traverses autonomously at `step_budget` cost along walkable BFS paths (never void; animation follows the path). **Fog-of-war (Phase 2.5):** situations AND terrain tiles hidden until discovered; discovery tied to `explored_cells` (**tile discovered ⟺ situation revealed → stage always completable**); **frontier exploration** (4-tier: discovered objective → light-biased discovered node → nearest-unexplored frontier → re-offer objective once map fully explored). Directives reframed to **exploration-style** (Scout: short steps / wide `reveal_radius` / safe escape / intel retention; Seek: long steps / narrow radius / high exposure) — 100% data-driven, **no directive IDs in GDScript**. **Pass→`passed`** lets you keep exploring; passed nodes stay visible; objective re-offered only at full exploration. Durable run-state: explored/revealed/resolved/passed survive return-home + re-entry (+ a different party). 3-state tile fog (void=empty, discovered=clear, undiscovered=dim FogLayer tile, swappable for art). **NEW RNG (append-only):** `stage.N.explore.terrain.*`, `…sit.N.wpos.*`, `…plateau.N.shape`; reveal re-keyed `stage.reveal.<sit_id>` (now unconditional — `reveal_threshold` removed). **Deferrals → later phases:** richer hidden-encounter/intel nuance; P3 distinct combat scenarios. |
| **V2-STAGE-004 Phase 3** — Per-Mode Combat Objectives: Recover / Protect / Endure + Movement & Actor-ID Fixes | Foundation | Done | `CombatState.gd`, `FlowEncounterState.gd`, `FlowRuntime.gd`, `EncounterContext.gd`, `EncounterRoundsState.gd`, `GridService.gd`, `StageTerrain.gd`, `SaveService.gd`, `ActorSchema.gd`, `EchoActor.gd`, `data/balance.json`, `data/realms.json`, `ui/AppRoot.gd`; new tests `ObjectiveCombatTests.gd` (18), `CombatRoundtripIntegrationTests.gd` (2) | **RECOVER:** relic (StructureActor, spawned deep) — win = hold_counter≥hold_rounds (echo adjacent to relic each round-end). **PROTECT:** entity (StructureActor, spawned mid-field) — win = round_counter≥duration_turns (survive); lose = entity_hp→0. **ENDURE:** deterministic wave spawn each round (`combat.wave.<id>.<round>`, append-only RNG, appended at END of initiative_order) — win = round_counter≥duration_turns. `all_enemies_defeated` stays universal FIRST win; COMBAT/PURIFY_SHRINE byte-identical. `resolve_objective_params(mode_key, mode_cfg, completion_index, stage_params)` (pure static, per-mode tuning from `data.combat.objective_modes`, float growth, clamped; stage `params` override). Unscouted-approach **surprise fear bump** (`data.combat.encounter_approach.surprise_fear`, fear only, no initiative re-sort). Enriched `objective_state` snapshot (hp/alive, round, rounds_required, hold_progress, hold_required). **Movement fix:** `GridService.move_toward` walkable-BFS now roots over the FULL walkable set (was occupied-minus, stalled movers). **Pathing fix:** `StageTerrain.next_step` tiebreaks equal-BFS-distance neighbours toward target (chebyshev→manhattan→stable), removing systematic top-left/up-left drift in both combat and exploration. **Actor-id fix:** `SaveService` repairs empty/duplicate echo ids at load; `ActorSchema.validate()` fails on empty id; `ActorSchema.has_all_required_fields()` for structural-only checks; `FlowEncounterState._ensure_unique_actor_ids` guards at assembly. Config additive: `data.combat.objective_modes`, `encounter_approach.surprise_fear`, `recover_relic`/`protect_entity` structures; `endure` added to `foundation_objective_pool` + both realms' `objective_pool`. **Suite: 710 tests, 710 passed.** **Known limitation (RESOLVED by Phase 3 Distinctiveness below):** ENDURE could end early if party cleared all enemies during a between-waves lull (`all_enemies_defeated` fires first). **Deferrals:** Pursue→Phase 3b (FleeBehaviorModule); Guide/Spirit Escort→Phase 3c (GuideBehaviorModule). |
| **V2-STAGE-004 Phase 3 — Combat-Mode Distinctiveness** | Foundation | Done | `core/actors/behaviors/BehaviorArbiter.gd`, `core/actors/behaviors/MeleeBehaviorModule.gd`, `core/combat/CombatService.gd`, `core/combat/CombatState.gd`, `core/runtime/FlowRuntime.gd`, `core/state/flow/states/venture/FlowEncounterState.gd`, `data/balance.json`, `ui/screens/combat/CombatBoardScreen.gd`, `ui/screens/combat/CombatTokenLayer.gd`, `ui/screens/venture/ResolveScreen.gd`; tests `BehaviorArbiterTests.gd`, `CombatRoundtripIntegrationTests.gd`, `CombatServiceTests.gd`, `ObjectiveCombatTests.gd` | **Mechanism:** per-mode "directive-weight context" injected additively into BehaviorArbiter (merged onto the player's directive `intent_weights`, never mutating it); dormant situational conditions activated; objective-seeking enemy reinforcement (RECOVER); ENDURE dual-win; PROTECT theft loop + proximity-gated win. COMBAT untouched. **RECOVER:** designated holder (deterministic: speed→agi→id) gets advance/hold directive weights; `objective_in_range` situational digs holder in when adjacent to relic; enemies get `prefer_objective_target`; relic-seeking reinforcement trickles from far edge (`reinforce_interval`/`reinforce_size`/`reinforce_group`/`reinforce_max_total`), appended to END of initiative_order (no re-sort, no new RNG draws); relic invulnerable + **no HP bar** (draw-gated on `is_objective_relic`). **PROTECT:** echoes interpose via `objective_threatened` situational + target overrides (intercept enemy nearest totem; focus-fire carrier when stolen); **theft** — enemy ending adjacent to unguarded totem rolls `theft_chance` (RNG `combat.theft.<encounter>.<round>`, ≤1 draw/round) → carries it + deals `double_damage_mult`; carrier death clears theft; echoes focus-fire to recover; **win now proximity-gated** — `protect_counter` advances only when a living echo is within `protect_guard_radius` (Chebyshev, default 2) of entity, **resets to 0 when unguarded** (mirrors RECOVER hold); win when `protect_counter ≥ duration_turns`; if `totem_stolen` at win → `totem_taken` defeat. **ENDURE:** rising-wave size curve (`wave_size_rising_step`, clamped to `wave_size_max`); `all_enemies_defeated` victory **suppressed until `all_waves_spawned`** (FIXES prior transient-lull early-win); `all_echoes_dead` defeat still fires in the lull. **PURIFY_SHRINE:** ring-defend `directive_intent_weights` for NON-purifier echoes only. **New `combat_state` fields:** `recover_holder_id`, `recover_reinforce_count`, `waves_spawned`, `total_waves`, `all_waves_spawned`, `totem_stolen`, `totem_carrier_id`, `protect_counter`. **New per-turn ctx fields:** `resolution_mode`, `totem_stolen`, `totem_carrier_id` (read by BehaviorArbiter). **New `objective_state` fields:** `objective_invulnerable`, `waves_remaining`, `wave_total`, `totem_stolen`, `protect_progress`, `protect_required`. **New balance keys** under `data.combat.objective_modes`: `recover.{directive_intent_weights, reinforce_interval, reinforce_size, reinforce_group, reinforce_max_total}`; `protect.{directive_intent_weights, objective_threatened_radius, theft_chance, double_damage_mult, protect_guard_radius}`; `endure.wave_size_rising_step`; `purify_shrine.directive_intent_weights`. And `data.actor.situational_muls.{objective_in_range, objective_threatened}` (real non-stub). **Suite: ~737 tests, all green** (pre-existing flaky `sanctum.party/toggle` excluded). **Known limitations:** `objective_threatened_radius` defaults to 3 in the arbiter — tuning the JSON has no effect until the balance key is threaded from `data.combat` into the arbiter's `_cfg`; RECOVER holder gets no directive weight on round 1 (designated at first end_round); PURIFY sharpen applies to non-purifier echoes only; a pre-existing PURIFY purifier-passivity bug (morale collapse → idle) is tracked separately. **Deferrals:** Pursue→Phase 3b (FleeBehaviorModule); Guide Spirit Escort→Phase 3c (GuideBehaviorModule). |
| **V2-STAGE-004 Phase 3c** — Guide Spirit Escort: GUIDE_SPIRIT mode (protect-in-place + escort-to-destination) | Foundation | Done (2026-07-05) | `core/combat/CombatState.gd`, `core/combat/CombatService.gd`, `core/state/flow/states/venture/FlowEncounterState.gd`, `core/runtime/FlowRuntime.gd`, `core/state/flow/FlowContext.gd`, `core/realms/ObjectiveModel.gd`, `data/balance.json`, `data/realms.json`, `data/bark/spirit_barks.json` (new), `ui/shells/RealmShell.gd`, `ui/components/EchoCardItem.gd`+`.tscn`, `ui/components/EchoCardSpiritAccent.tres` (new), `ui/screens/combat/CombatBoardScreen.gd`, `ui/screens/combat/CombatBoardScreen.tscn`, `ui/AppRoot.gd`; tests `ObjectiveCombatTests.gd`, `CombatRoundtripIntegrationTests.gd` | **GUIDE_SPIRIT wired end-to-end.** A **NameBank-named spirit** (`is_spirit: true`, id `guide_spirit_01`) spawns **deep** — as a `StructureActor` (non-joining escort, faction `"structure"`) or an `EnemyActor` with faction `"echo"` when it **joins the battle** (fully-active ally via BehaviorArbiter; `_spirit_damage_mul` 0.75 applied in `CombatService._resolve_melee`). **Seeded 50/50 rolls (append-only RNG):** `guide_mode` protect|escort (`combat.guide_mode.<enc>`); escort join (`combat.guide_spirit_joins.<enc>`); name+gender (`combat.spirit_name.<enc>`); destination = seeded random walkable **EDGE** cell, min-distance-guarded (`combat.spirit_destination.<enc>`); long board via `data.combat.board.guide_spirit_override.long_multiplier: 5.0` (`combat.guide_spirit_board.<enc>`, both orientations). **PROTECT variant:** skittish spirit flees 1 deterministic away-step when an enemy is within `skittish_radius` (3) and no echo adjacent; win = **guard-to-count** — `guide_protect_counter` accumulates (NEVER resets) on rounds a living echo is within `escort_radius` (2) of the spirit; win at `duration_turns`, reason `"spirit_protected"`, sets `guide_spirit_protected: true` in the final snapshot (V2-ITEM-002 free-summon seam, flag only). **ESCORT variant:** spirit steps 1 cell/round via `StageTerrain.next_step` toward the destination only when `escort_started` (first-adjacency latch) AND a living echo is within `escort_radius`; occupied next cell → waits; win `"spirit_escorted"` (`destination_reached`, priority over `all_enemies_defeated`). **Both:** spirit killed → `"spirit_killed"` defeat (early guard, priority over kill-win); a joined spirit is excluded from `all_echoes_dead`. **End-priority ladder:** 0 PURSUE quarry_escaped → **0b GUIDE_SPIRIT spirit_killed / spirit_escorted** → 1 all_enemies_defeated → … → 9 protect guard-to-count win. **New `combat_state` fields:** `guide_mode`, `spirit_id`, `spirit_joins_battle`, `escort_started`, `destination_col`, `destination_row`, `destination_reached`, `guide_protect_counter` (guide_mode/joins/destination seeded via `objective_params` into `create()`). **New `objective_state` fields:** `guide_mode`, `spirit_alive`, `spirit_hp`, `spirit_name`, `spirit_joins_battle`, `destination_reached`, `destination_pos`, `rounds_remaining` (= duration − guide_protect_counter). **Barks:** `data/bark/spirit_barks.json` (4 contexts: `spirit_first_adjacency`, `spirit_escort_start`, `spirit_guide_win`, `spirit_killed`), loaded via `_load_spirit_barks()` (contact_responses lazy-load pattern), fired via `_fire_spirit_bark` (posmod variation) + `round_bark_events`. `resolve_objective_params` `"guide_spirit"` arm propagates ALL balance keys (no #33 silent no-op). **Config:** `objective_modes.guide_spirit` block + `board.guide_spirit_override` + `data.stages.objective_types.guide_spirit` UI entry; `pursue` + `guide_spirit` appended to both realms' `objective_pool` + the foundation pool. **UI:** RealmShell EchoBar renders the spirit as a distinct **last slot** (`EchoCardItem.setup_spirit()`: ◆ SPIRIT badge, gold accent StyleBox `EchoCardSpiritAccent.tres`, HP + progress line); `CombatBoardScreen` camera **pan** (2-finger gesture AND single-pointer click/tap-drag in `_gui_input`, 8px threshold + mouse/touch source-lock) + **pinch zoom** now on **ALL** combat modes (was PURSUE-only), isometric-accurate pan clamp (`_board_span_px`), ⌖ RecenterButton (party centroid); CombatTokenLayer draws the spirit **gold halo/nimbus** (distinct from PURSUE's solid diamond), tunable via CombatTokenVisualConfig exports, spirit HP bar NOT suppressed. **Dev tooling:** `combat_objective guide_spirit [protect|escort] [join|nojoin]` (`FlowContext.dev_guide_mode`/`dev_guide_joins`, draw-then-override preserves RNG order). **Suite: 815 tests, 815 passed.** **Known limitations:** escort has no timeout (a permanently blocked path — shouldn't occur given `StageTerrain` ≥2-wide bridges — would stall); `get_threatened_ally` may pick the joined spirit as a guard target (accepted as thematic); a joined spirit's arbiter move intents run but board movement is owned by `_end_round` only for STRUCTURE spirits (joined spirit moves freely). **Deferral:** free-summon reward wiring → V2-ITEM-002 (the `guide_spirit_protected` flag seam exists). |
| **V2-STAGE-004 Phase 5** — Explore + Combat UI/UX Surface Pass (Anansi event snippets, journey barks, frontier chaining, mid-path stop) | Foundation | Done (2026-07-09) | **Explore UI:** `ui/screens/venture/StageExploreScreen.gd`+`.tscn`, `StepProgressBar.gd` (new), `DirectiveBadge.gd` (new), `GhostFootprintLayer.gd` (new), `PartyTokenLayer.gd`, `SituationMarkerDraw.gd`; **Combat UI:** `ui/screens/combat/CombatBoardScreen.gd`+`.tscn`; **Backend:** `core/state/flow/states/venture/FlowStageExploreState.gd`, `core/runtime/FlowRuntime.gd`, `core/state/flow/states/venture/FlowEncounterState.gd`; **Data:** `data/balance.json` (`data.stages.anansi_snippet_events`), `data/shouts/journey.json` (new `journey.travel` context), `data/stages/anansi_travel_snippets.json` (new — 24 lines); `ui/AppRoot.gd` (test reg); tests `tests/StageExploreP5Tests.gd` (new), `tests/TraversalModelTests.gd` (+4 traversal P5 tests) | **Suite: 836 tests, 836 passed.** UI/UX surface pass over Explore + Combat, plus two approved traversal fixes. **Explore UI:** `StepProgressBar` (kente segments = ACTUAL steps this advance, depleting per tile with the travel tween; at rest shows full budget; "Steps x/y" readout, y = active directive's `step_budget`, data-driven); `DirectiveBadge` (glyph+label+tint, dict-driven: Mist Blue scout / Akan Gold seek); TravelSnippetLabel (Anansi ghost text, cream + dark outline ~17.5:1); travel echo barks via instanced BarkPopupLayer above the party token; reveal scale-pop on markers; `GhostFootprintLayer` (fading trail on traveled cells, board-parented); engagement-popup choice CTAs for obstacle/structure (`situation_pending.choices` → `stage.resolve_situation_choice`; >2 choices warned+dropped); 200ms transition flash into combat-track engagements. **Combat UI:** authored `ObjectiveBanner` with **7 per-mode layouts** (combat/purify/recover/protect incl. `entity_name` + STOLEN urgent state/endure/pursue incl. window + quarry-distance pips/guide_spirit instruction-only); top-chrome de-overlap (round label top-center, banner below, initiative pushed down, pace controls stacked bottom band); palette fix (theme-default dark-brown labels were 1.4:1 on dark panel → cream/gold chrome, all pairs ≥5.33:1); `RoundLabel` cream + 4px dark outline (~17:1, board-independent). **Backend snapshot (`flow.stage_explore`, all additive):** composite `directive {id,label}`; `party_preview[].emotional_status`; `situation_pending.choices[]` (obstacle/structure only); transient `travel_bark {actor_name,line}` + `travel_snippet` (cleared each advance; UI de-dupes replay via last-played gate). **Echo travel barks:** ShoutBank `journey.travel` context (NEW content: 9 archetypes × 4 bands × 3 lines in `journey.json`), odd-t cadence, variation-key selection, **no RNG**. **Anansi snippets are EVENT-DRIVEN (not cadence):** four moments — first stage entry, objective revealed on advance, all required objectives complete, failed return-home — each togglable via NEW `data.stages.anansi_snippet_events`; pool = NEW `data/stages/anansi_travel_snippets.json` (moment-agnostic "travel" pool today; per-event-pool seam commented in `_load_anansi_snippet_pool`); deterministic `posmod(t)` line pick; `objectives_complete` one-shot via durable `explore_map.anansi_complete_fired`. **Traversal fix — frontier chaining:** for **tier-3 frontier targets only**, fog lifts per step and the target re-computes (`nearest_unexplored`) when reached, chaining until `step_budget` is spent (previously advances stopped ~`reveal_radius`+1 ≈ 2 tiles out under Seek; budget now binds). Tier-1/2 (objective / discovered-node) targets keep hard-stop-on-arrival. **Traversal fix — mid-path stop:** stepping ONTO an unresolved, un-passed situation parks the party with `pending_situation_id` (canon: walking onto any situation triggers resolution); passed/resolved cells walk through — Pass invariants preserved. **Hygiene note:** `travel_bark`/`travel_snippet` strings persist in the saved `explore_map` (same accepted lifecycle as `last_traveled_path`); never re-emitted on Continue. **Deferrals:** Phase 4 (conversation↔combat seams) remains. |
| **V2-STAGE-004 Phase 3a** — Irregular Combat-Board Foundation (seven-mode combat family) | Foundation | Done | `EncounterResolutionModes.gd` (seven canonical consts, names=values 1:1 with objective types: COMBAT/PURIFY_SHRINE/RECOVER/PROTECT/ENDURE/PURSUE/GUIDE_SPIRIT; SURVIVAL→ENDURE, PROTECT_TOTEM→PROTECT renamed, RECOVER/PURSUE new), `StageTerrain.gd` (`generate(... , rng_namespace="")` — combat reuses the exploration generator under append-only `combat.terrain.<id>.*`; default namespace byte-identical), `GridService.gd` (walkable-aware `place_actors` + BFS-based `move_toward` reading `board_cfg["walkable"]`; empty ⇒ legacy), `EncounterContext.gd` (`terrain` field, transient), `CombatState.gd` (`create(... , objective_params={})`), `FlowEncounterState.gd` (terrain gen at `enter()` per realm virtue signature + completion-order board sizing; walkable shrine placement with depth scaled by completion order; `terrain`+board dims in snapshot), `FlowRuntime.gd` (`_resolve_next_actor` threads `walkable` into movement board_cfg), `CombatBoardScreen.gd` (paints only walkable cells, void=no tile, no fog), `balance.json` (additive `data.combat.board` sizing + `data.combat.objective_placement` depth scaling), `AppRoot.gd` (test reg), `docs/combat-modes.md` (new — authored mode design specs), `tests/CombatTerrainTests.gd` (10 tests) | **674 total tests, 673 passed (1 pre-existing flaky `party_toggle`).** Combat boards are now **irregular StageTerrain landscapes** keyed to the realm's virtue signature (same generator as exploration), **sized by realm completion order** (`data.combat.board`: base 12×12 → +1/completion → max 22×22). Actors spawn on **walkable cells only**; combat movement routes over land/bridges, **never into void**. **Regression keystone: empty/absent `board_cfg["walkable"]` ⇒ byte-identical legacy 10×10** (tests / keeper_intro / no-active-realm). Seven resolution-mode consts are 1:1 with objective types (one `combat` family). Shrine placement depth scales with completion order (early realm central/reachable → late realm deep among enemies). **NEW RNG (append-only):** `combat.terrain.<encounter_id>.*` (existing `combat.placement.<id>` order untouched; walkable placement makes zero rng draws). Authored per-mode design mechanics live in `docs/combat-modes.md`. **Deferrals:** per-mode win/lose conditions + objective-actor spawn → Phase 3 (Recover/Protect/Endure), 3b (Pursue + FleeBehaviorModule), 3c (Escort + GuideBehaviorModule); terrain organic-ness tuning (boards still a bit rectangular) → dedicated follow-up; placement-modifier weights on terrain placement → follow-up. |
| **V2-STAGE-004 Phase 4** — STAGE-003 Conversation→Combat Seams + Earned Return Recruitment + Contribution Ledger (final phase — story now Done) | Foundation | Done | `core/actors/ContactActorBuilder.gd` (new), `core/sanctum/RecruitmentService.gd` (new), `data/bark/ally_barks.json` (new), `core/state/flow/states/venture/FlowEncounterState.gd`, `core/combat/CombatState.gd`, `core/combat/CombatService.gd`, `core/state/encounter/EncounterContext.gd`, `core/runtime/FlowRuntime.gd`, `core/state/flow/FlowContext.gd`, `core/state/flow/states/sanctum/FlowSanctumState.gd`, `core/state/flow/states/sanctum/FlowEchoPartyState.gd`, `core/realms/SituationResolutionService.gd`, `ui/screens/sanctum/SanctumScreen.gd`+`.tscn` (new `%CompanionInvite` modal), `ui/screens/venture/ResolveScreen.gd`+`.tscn` (layering fix), `ui/components/EchoCardItem.gd`+`.tscn` (new `EchoCardAllyAccent.tres`), `ui/screens/sanctum/EchoPartyScreen.gd`+`.tscn`, `ui/AppRoot.gd` (4 dev commands + test reg), `data/balance.json` (`data.contact.ally`, `data.contact.recruitment`, `data.contact.claimant.combat_intro_line`, `data.combat.charge_pressure`, `data.actor.enemy_types.temporary_ally`); new tests `tests/RecruitmentServiceTests.gd`, `tests/ContactActorBuilderTests.gd`, `tests/Stage004SeamTests.gd`; extended `tests/CombatStateTests.gd`, `SituationResolutionServiceTests.gd` | **882 total tests, all passing.** Closes the last STAGE-004 gap: STAGE-003 conversation outcomes now reach combat. **Temporary Ally auto-join:** `ContactActorBuilder.build()` mints an echo-faction combat actor (`faction:"echo"`, `is_ally:true`, `_ally_damage_mul`) from a good `temporary_ally` contact; injected party-side on a walkable cell in `FlowEncounterState.enter()`; excluded from `all_echoes_dead` (`CombatState`, mirrors the P3c joined-spirit exclusion); consumed after ONE battle (`ally_consumed_in_encounter`); ally death → small party morale/fear knock (excludes spirit/ally recipients). Visual: ⊕ ALLY Mist-Blue (`#7AB5C8`) badge + board token ring (`EchoCardAllyAccent.tres`). **Failed Claimant → immediate combat:** sets `active_encounter_objective_index=-1` (forces COMBAT) + `explore_map.combat_intro_reason="claimant_hostile"` → transitions to `flow.encounter`; PrebattlePanel shows `combat_intro_line` from `data.contact.claimant.combat_intro_line`. **Failed non-objective Charge → pressure:** sets `explore_map.hostile_charge_sit_id`; consumed once in `FlowEncounterState` objective-params (protect `+protect_duration_bonus` duration_turns / endure `+endure_wave_bonus` wave_size, clamped); ObjectiveBanner "Pressure raised" Amber marker on `objective_state.charge_pressure_applied`. **Earned Return recruitment:** `RecruitmentService.compute_recruit_chance()` (base 0, additive conversation+combat+fit, clamp [0,75], components rescaled via largest-remainder so they always sum to `chance`), `.roll()` (single seeded draw, namespace `combat.<enc>.ally_recruit`, honors `dev_force_recruit`), `.promote_ally_to_echo()` (mints a roster echo `origin:"recruited_ally"`, `rarity:"uncalled"`, Standing 1, stats re-derived; seeds a NEGATIVE companion bond debuff vs every existing roster echo). Gated: ally ALIVE + encounter VICTORY. **Companion invite is a SANCTUM EVENT (design change from the original Resolve-panel plan):** a successful roll writes durable `sanctum.companion_invite` (ONE max — no-stack guard discards a second while one pends); `FlowSanctumState` projects `data.companion_invite`; the `%CompanionInvite` modal on `SanctumScreen` shows it on entry and PERSISTS until decided via `sanctum.companion.accept`/`sanctum.companion.decline`. **REMOVED from Resolve** — the old `cta.recruit_accept/decline` + `ally_recruit_offer` resolve projection are gone. **Combat Contribution Ledger (Tier 1):** `EncounterContext.echo_action_logs` generalized to BOTH factions with `damage_dealt`/`damage_taken`/`kills` (populated at the single melee choke; `kills` derived from `defender_hp_after<=0`); projected as a `contribution` sub-dict in the final snapshot; `ProgressionService` byte-identical (keyed lookup). **Companion origin tag:** ⊕ Companion badge on every Sanctum echo surface (SanctumScreen hub strip + echo detail; EchoPartyScreen list/cards/detail), driven by the additive `echo.origin` field. **Debug commands (F1, dev-only):** `spawn_ally`, `force_claimant_combat`, `force_charge_pressure [on|off]`, `force_recruit [success|fail|clear]` (`FlowContext.dev_force_recruit`, draw-then-override preserves RNG order). **guide_spirit routing fix (pre-existing Phase 3c soft-lock, folded in):** `guide_spirit` added to `SituationResolutionService._ASYNC_OBJ_TYPES` so a guide_spirit OBJECTIVE routes to real combat and completes (previously fell through to flavor `in_explore` and never set `completed`, soft-locking stage advancement). **ResolveScreen layering fix:** card content wrapped in a bounded `ScrollContainer`; resolve overlay raised above the EchoBar. **New save fields (additive):** `explore_map.{ally_contact, ally_contact_id, ally_consumed_in_encounter, hostile_charge_sit_id, combat_intro_reason, ally_recruit_rolled_encounter_id}`, `sanctum.companion_invite`, `echo.origin`. **New action types:** `sanctum.companion.accept`, `sanctum.companion.decline`. **Visual language:** ally/companion = Mist Blue `#7AB5C8` + ⊕ Odo Nnyew glyph (distinct from spirit gold, enemy red). **Deferrals:** Tier-2 support-attribution ledger; `is_kill` dead-code fix (pre-existing); recruitment-config de-duplication; venture-screen layering/safe-area audit; in-explore objective-completion guard (defense-in-depth); plus the original STAGE-004 deferrals — item rewards → V2-ITEM-002, enemy pressure roles → V2-COMBAT-002. |
| **V2-STAGE-004 — ALL PHASES SHIPPED** | Foundation | **Done** | P0–P3c (PRs #27/#28/#29/#30/#33/#36/#37), P3a, P5 (#38), P4 (this branch) | Umbrella story is now fully closed. Phases: 1 (unified situation resolution + resolve surface), 2 + 2.5 (movement-cost traversal, irregular maps, data-driven directives, fog-of-war), 3 (per-mode combat objectives Recover/Protect/Endure), 3 Distinctiveness (per-mode behavior), 3a (irregular combat-board foundation, seven-mode family), 3b (Pursue), 3c (Guide Spirit escort), 5 (Explore + Combat UI/UX surface pass), 4 (conversation→combat seams — this row, final). Remaining forward-looking deferrals from across all phases: V2-ITEM-002 (loot_results seam, free-summon seam), V2-COMBAT-002 (enemy pressure roles), Tier-2 support-attribution ledger, recruitment-config de-duplication, venture-screen layering/safe-area audit, in-explore objective-completion guard, terrain organic-ness tuning, `flow.resolve` overlay tech-debt (see `CONVENTIONS.md` Shell Routing Model). |

---

## Foundation Wave — Social Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **BOND-001** — Bond trigger | Foundation | Done | `SocialGraphService.gd`, bond scoring | Signed strength edges |
| **BOND-002** — Bond incidents + rival | Foundation | Done | `rival_incidents` tracking, `grief`/`shared_survival` modifiers | Rival pairs seeded for V2-SANCTUM-005+ |

---

## Foundation Wave — Combat Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-COMBAT-001** — Readiness score, hesitation band, defeat bug fix, enemy count scaling | Foundation | Done | `CombatState.gd`, `ActorStateMachine.gd`, `FlowEncounterState.gd`, `FlowRuntime.gd`, `balance.json`, `tests/CombatInitiativeTests.gd`, `tests/CombatConsequenceTests.gd` | Morale-tier initiative modifier (inspired +4 … broken −6); "hesitating" status band (fear ≥ 40); guard self-set fix; defeat re-entry bug fixed (encounter_ctx nulled on defeat path); enemy count config-driven via spawn groups (1/2/3 base by realm completion index); 8 new bidirectional emotion triggers (guard absorb, near-death, shrine purify, outnumbering, witness refuse, overwhelmed, consecutive no-damage). V2-COMBAT-002 required for tonal realm differentiation (type variety). V2-INFRA-005 required before permadeath. |
| **V2-COMBAT-002 Slice 1** — Shared Movement Contract + Path/Terrain Foundation | Foundation | In Progress (Slice 1 accepted) | `core/movement/contracts/` (Context/Profile/Goal/Option/Intent/Event/Result + validation), `core/movement/MovementPathService.gd`, `core/realms/StageTerrain.gd` (`is_legal_edge`, `legal_neighbors`), `tests/MovementContractTests.gd`, `tests/MovementPathTests.gd`, extended `tests/StageTerrainTests.gd`, test registration | Deterministic contract and routing foundation only: exact deep-copied data shapes, stable diagnostics, paths exclude origin, capacity envelope 0–6, weighted destination-entry costs, insertion-order-independent tie policy, and shared 8-direction topology. Approved diagonal rule: block only when both orthogonal side cells are solid; occupancy never counts as terrain solidity. **No live combat/exploration behavior switch and no live capacity derivation in Slice 1.** **Verification:** 1011/1011 full suite; contracts 33/33; paths 10/10; terrain 27/27; combat terrain 16/16; Jeff manual regression pass — everything works as before. **Deferred:** approved slices 2–6. |
| **V2-COMBAT-002 Slice 2** — Dormant Pressure, Route Options + Complete-Candidate Arbitration | Foundation | In Progress (Slice 2 accepted, PR #46) | new contracts `MovementActionPlan`, `MovementPerceivedActorFact`, `MovementKnownHazardFact`, `CombatPressureSnapshot`; tightened Context/Goal/Option/Intent; `CombatPressureService.gd`, `MovementOptionService.gd`, directed-edge extension in `MovementPathService.gd`, dormant `BehaviorArbiter.select_movement_intent()`, `balance.json` (`data.combat.movement.spatial_utility`), four new Slice 2 suites, test registration | Fresh-base exact contracts require perceived facts and explicit action plans/fallbacks. Seven-mode pressure produces bounded direct/tactical/safety goals without taking objective authority. Options use perceived-and-walkable topology, terrain plus hostile-control edge costs, bounded slack, and truthful progress/exposure/cohesion/congestion/hazard metrics. Complete-candidate arbitration preserves visible legacy identity, Calling, virtue, emotion, Directive, maturity, skill, vow, and bond layers, then adds capped spatial utility; purifier authority remains exact. **Dormant only:** production `select_intent()`, FlowRuntime, ActorStateMachine, EncounterContext, initiative, objective/win authority, snapshots, stage movement, and UI behavior are unchanged. **Verification:** 1081/1081 full suite; focused contracts 12/12, pressure 23/23, options/path 23/23, arbiter/legacy 70/70; Jeff manual regression approved unchanged live behavior. **Merged:** Slices 1 + 2 landed on `main` as PRs #45 / #46. **Deferred:** retreat/interpose/protect/pursue/carry to Slice 4, party stage adapter to Slice 5, and playable presentation to Slice 6. |
| **V2-COMBAT-002 Slice 3** — Profile Derivation, Shared Executor, Fixed Hazards + Ordered Activation | Foundation | In Progress (Slice 3 accepted, dormant) | `core/movement/MovementProfileService.gd`, `core/movement/MovementHazardService.gd` (+ `MovementHazardFixtures.gd`), `core/movement/MovementExecutor.gd`, `core/movement/CombatActivationService.gd` (all new, pure/static); additive `HAZARD_TYPES` const in `MovementKnownHazardFact.gd`, `data.combat.movement.{capacity,hazards}` in `balance.json`, Slice-3 test registration in `AppRoot.gd`; new suites `MovementProfileTests.gd`, `MovementHazardTests.gd`, `MovementExecutorTests.gd`, `CombatActivationTests.gd` | **Capacity FROZEN:** `final = clamp(max(standing_capacity, aptitude_capacity), 2, 6)` — a `max()`, not a sum. Standing bands S1–2→2 / S3–5→3 / S6–9-and-above→4 (ceiling 4, S9 no auto-increase); aptitude = `2 + (agi≥12:+1)+(agi≥18:+1)+ Calling kra_soro(+1)+ skill kra_soro_open_ground(+1)` (no Standing term; `sum_okwanfo_shadow_step` deferred; no saved `max_movement`; equipment→V2-ITEM-003). Structures→0; non-joining GUIDE spirit→capacity-1 only via **explicit** `options.authored_override` (never inferred from `is_spirit`). **Hazards:** three fixed types (unstable/binding/burning), `resolve_cell_entry` + `resolve_end_activation`, once-per-type-per-activation ledger; Unstable displacement ranked outward-progress→angular-deviation (no row/col bias; genuine tie→`fallback_damage`); forced displacement free + can't recursively retrigger Unstable. **Executor:** pure edge-by-edge; two-solid-corners diagonal via `StageTerrain.is_legal_edge`; dynamic occupancy; once-per-edge hostile-control surcharge (+1, sorted); ordered events; stop reasons reached_destination/commitment_spent/capacity_spent/no_route/blocked_edge/occupied/binding_stop. **Activation:** executor → action revalidation at final cell → purpose-restricted fallback → declared resolved action (declared, NOT executed) → Burning end-activation → KO/death truth → one validated `MovementResult`. The `3/3` hazard damages are **PROPOSED DEFAULTS** pending Jeff's Slice-6 tuning. **Dormant only** — nothing wired into live combat; live cutover is Slice 6. **Verification:** 1149/1149 full suite (baseline 1083 → 1149), 0 failed; compile clean; five independent reviews all SHIP (0 blockers/majors). **Slice-6 carry-forward guardrails (surfaced by review — NOT Slice-3 defects):** reconcile `controlling_state` default between `MovementExecutor` (false) and `MovementProfileService` (true); guard `hazard_ctx.config` presence at the live call site; drive `mover_ko_only` from live actor state; ratify `_PURPOSE_FALLBACK_ALLOW`; ensure the live caller declares the guard/observe fallback; confirm the `3/3` hazard damage against tuning. **Deferred:** carry/retreat/interpose/protect/pursue → Slice 4, party stage-movement adapter → Slice 5, playable presentation + live cutover → Slice 6. |
| **V2-COMBAT-002 Slice 4** — Escape Authority, Guide-Spirit Activation, Protect Custody + Board Fall-Back | Foundation | In Progress (Slice 4 accepted, dormant) | `core/movement/PursueEscapeService.gd`, `core/movement/GuideSpiritActivationService.gd`, `core/movement/ProtectCustodyService.gd` (all new, pure/static); additive extensions to `core/movement/CombatPressureService.gd` and `core/movement/MovementExecutor.gd`; `data/balance.json`; Slice-4 test registration in `ui/AppRoot.gd`; new suites `PursueEscapeTests.gd`, `GuideSpiritActivationTests.gd`, `ProtectCustodyTests.gd`, `SpatialModeGoalTests.gd`, extended `CombatPressureTests.gd` + `MovementExecutorTests.gd` | **Escape authority unified:** `PursueEscapeService` is the single long-axis escape seam (`escape_cells`/`is_escaped`/`escape_graph`/`cutoff_cells`). The `>= max-1` **band** is the one predicate (`_is_escape_geometry`) and `is_escaped` **byte-replicates** `FlowRuntime.gd:2082-2087`, so the Slice-6 cutover is behavior-preserving; **four** previously inconsistent inline escape computations collapse into one. `cutoff_cells` projects a shortest-route corridor off the escape graph (`cost_to_quarry + cost_to_escape == min_escape_cost`), filtered to cells a pursuer reaches no later than the quarry. **PURSUE quarry timing/economy PRESERVED — only cutoff *targeting* changed; the escape-graph cutoff is PRIMARY over the authored `fallback_region`.** **Guide spirit:** `activate_spirit(spirit_actor, context, guide_state, hazard_ctx, capacity_cfg)` runs the non-joining spirit's objective-phase movement through `CombatActivationService` + hazards; authored one-cell pace via `authored_override` capacity 1; hard `joined` precondition refuses joined spirits; escort = first step of `shortest_path`, skittish = neighbour ranked Chebyshev → Manhattan → threat-distance vector → obstruction-distance vector → numeric (reflection-covariant); scores **no** objective progress (forced `0.0`). **Custody:** immutable `custody_state`; **pickup IS the activation's single primary action** (`protect.totem_pickup`, `pickup_range` 1); totem follows the carrier across voluntary **and** forced steps; −1 carrier capacity burden respecting the capacity-1-requires-`authored_override` invariant; theft transfers only on a successful **ATTACK** (injected roll, strict `<`, **no internal RNG**) and **never** on cell entry (explicit tested no-op); enemy carrier move/action-restricted + double damage via the existing `double_damage_mult`; drop/recovery cell = carrier's current cell. **Board fall-back reuses the existing `withdraw` purpose** — no new purpose/style invented, `RetreatService` untouched ("withdraw from combat entirely" stays out of scope). **Executor:** authored-override movers get a one-**STEP** pace independent of edge cost (`step_allowance = mini(authored_override.capacity, commitment)`, counted in EDGES), so a single edge is always affordable regardless of terrain cost or the `+1` hostile-control surcharge; normal movers keep byte-identical cost-budget semantics; `voluntary_cost` records true spend; `remaining_capacity` clamps at 0. **This pinned a confirmed bug:** under a cost budget the non-joining spirit could never move with a hostile adjacent or terrain cost ≥ 2 — PROTECT was inert exactly when it mattered, a regression vs V1's cost-free BFS. The rule keys off the `authored_override` **contract**, so it also covers burdened PROTECT carriers (incl. enemy carriers capped to 1) — Jeff explicitly confirmed this broader application (`docs/movement-model.md` §13.7). **Action economy UNCHANGED** (capacity = movement only; one primary action after movement); the proposed unified action-point pool is deferred to its own story — see `docs/proposals/action-point-economy.md` (untracked planning area). **Dormant only** — nothing wired into live combat; live cutover is Slice 6. **Verification:** **1233/1233 full suite** (baseline 1149 → 1233, +84), 0 failed; compile clean; five independent reviews (architecture, determinism, movement-design, mode-correctness, test-coverage) plus a **targeted second pass** over `GuideSpiritActivationService` and the four new suites, which caught and fixed three genuine defects — (a) a malformed ternary making `escape_cells` return the far-end line instead of the band, (b) the authored-pace step-count pin, (c) two un-reverted mutation-test edits left in the tree. Jeff's manual regression pass confirmed nothing regressed. **Slice-6 carry-forward (surfaced by review — NOT Slice-4 defects):** `GuideSpiritActivationService` declares purpose `escort`/`protect` with an `actor.idle` primary, which is **invalid** under `MovementGoal`'s own primary-action rules (escort requires `protect_ally`; `actor.idle` is legal only under `read`) — reconcile, or add a `guide_spirit` purpose, before a Slice-6 caller builds a real `MovementGoal`; `"no_route"` conflates **four** outcomes (caller-gated, joined-refusal, arrived, unreachable) and "arrived" vs "unreachable" are opposite facts a GUIDE scorer must distinguish; the joined-refusal path still calls `activate`, so Burning still resolves at origin and a mis-branched caller could double-bill; `COLLAPSE_HEALTH` (0.5), `FALLBACK_RADIUS` (3), and `INTERCEPT_LANE_RADIUS` (3) are hardcoded and need a `balance.json` seam before governing live movers; `withdraw`'s §8.3 action consequence (usually forfeits attack) is currently unowned by any slice; the `controlling_state` default still diverges (`MovementExecutor` false vs `MovementProfileService` true); guard `hazard_ctx.config` presence (currently fails soft to zero damage); drive `mover_ko_only` from live actor state; ratify `_PURPOSE_FALLBACK_ALLOW`; confirm the proposed 3/3 hazard damages; add a **fidelity test** so the escape rule cannot silently drift from `FlowRuntime.gd:2082-2087`; untested paths remain `resolve_pickup` "mover_downed", `track_carrier_movement` `carrier_downed`, and `precludes_attack` on an invalidated pickup. **Deferred:** party stage-movement adapter → Slice 5, playable presentation + live cutover → Slice 6. |
| **V2-COMBAT-002 Slice 5** — Party Stage Adapter (dormant) + `traveled_path` Convention Correction (live) | Foundation | In Progress (Slice 5 accepted) | `core/movement/StagePartyMovementAdapter.gd` (new, pure/static/dormant); additive `"explore"` entry in `core/movement/contracts/MovementGoal.gd` `MODES`; **live path-convention correction** in `core/runtime/FlowRuntime.gd`, `core/state/flow/states/venture/FlowStageExploreState.gd`, `core/save/SaveService.gd`, `ui/screens/venture/StageExploreScreen.gd`; Slice-5 test registration in `ui/AppRoot.gd`; new suite `tests/StagePartyMovementTests.gd` (23 tests), extended `tests/TraversalModelTests.gd` | **1256/1256 full suite** (baseline 1233 → 1256, **+23**), 0 failed. **Everything dormant EXCEPT one authorized live correction.** **Adapter:** converts party/directive state into ONE shared `MovementProfile` / `MovementGoal` / `MovementIntent` / `MovementResult` for STAGE traversal (`build_profile`/`build_goal`/`build_intent`/`build_result`/`select_frontier`/`select_objective_target`). Pure static, no RNG, no `OS` time, zero references to `FlowRuntime`/`FlowContext`/`SaveService`/UI, **zero production callers**. **ONE PARTY TOKEN** (`MOVER_ID = "party.token"`) — the API exposes no per-Echo seam by design. Capacity is the directive `step_budget` (Scout 3 / Seek 6), NOT the clamp(2,6) combat formula; a budget below 2 emits an `authored_override` naming `directive.step_budget` instead of silently clamping up. `controlling_state` false. Reachability goes through `MovementPathService`, so the shared two-solid-corners diagonal rule applies — `StageTerrain.bfs_distance_field` is **deliberately NOT used** (it expands neighbours with a raw `walkable.has()` and ignores that rule). **`MovementGoal.MODES` += `"explore"`** — allowlist append, cannot invalidate an existing goal; replaces the false `recover` segment in stage `goal_id`s. **LIVE correction — `traveled_path` now EXCLUDES the start cell**, matching `MovementContractValidation.require_path_excludes_origin`. New additive snapshot field `data.traveled_origin` and save field `explore_map.last_traveled_origin` carry the pre-move cell; the UI tween was re-anchored (segment 0 departs from `traveled_origin`, `seg_count = traveled_path.size()`). **Rendered animation, ghost trail and step-diamond count are unchanged.** Save change is **ADDITIVE** — `last_traveled_path` is neither removed nor renamed; `last_traveled_origin` defaults to `{}` with a `SaveService` repair, and the snapshot projection falls back to `party_pos`. Live exploration ROUTING is untouched (`StageTerrain.nearest_unexplored`, `StageTerrain.next_step`, `FlowRuntime._find_explore_target` tiers 1–2) — the corrected rules go live at the Slice-6 cutover. **Slice-6 carry-forward (surfaced by review — NOT Slice-5 defects). These are IN ADDITION to the Slice-3 and Slice-4 carry-forward lists above, which all remain in force:** **TOP RISK — `heading` is not plumbed anywhere in `core/`.** Until it is, the frontier tie-break's final determinism guard governs selection on symmetric boards and is directionally biased: it is a lexicographic STRING compare of `"col,row"` (`"10,3" < "9,3"`), so it is neither row-major nor col-major and the favoured direction flips with coordinate magnitude. A characterisation test pins the current biased answers — it must be **replaced, not "fixed"**, at cutover. Then: `MovementGoal`'s purpose/plan validation rules are combat-shaped — `advance` requires its `target_id` to also appear in `relevant_actors`, so all moving stage goals currently emit `reposition` with tier ordering carried by `urgency` (same family as the known Slice-4 `escort`/`protect` + `actor.idle` invalidity; Jeff approved deferring the contract fix to Slice 6); the bounded-slack constants (`SLACK_FLOOR` 2 / `SLACK_FRACTION` 0.25) duplicate the inline rule at `MovementOption.gd:138` — unify behind a `balance.json` seam; `StageTerrain.bfs_distance_field` ignores the diagonal rule (raw `walkable.has()`); dual `step_budget` resolution (`FlowRuntime` vs `FlowStageExploreState`, two independent `3` fallbacks that can silently diverge); the `dist_field`-empty branch teleports the party straight to target; the `stage.is_empty()` early-exit does not refresh the snapshot; live `next_step` uses `is_walkable` rather than the shared legality rule, and live `nearest_unexplored` has row/col bias; `build_intent` does not enforce path-excludes-origin; `build_result` sets `resolved_action = planned_action` unconditionally; the manhattan criterion introduces axis-aligned anisotropy (diagonal frontiers deprioritised); `select_objective_target` order-independence assumes unique situation ids, and its docstring should be narrowed to `reachable_cost_region` costs only; the legacy-save prepended-path × repaired-`{}`-origin interaction is untested; the `_003_repaired` flag is not set by the `last_traveled_origin` repair (mirrors its sibling). **Deferred:** playable presentation + live cutover → Slice 6. |
| **V2-COMBAT-002 Slice 6 (6A–6E)** — Live Cutover, Token Path Animation + Selector Repair | Foundation | **Done** | `core/runtime/FlowRuntime.gd`, `core/actors/behaviors/BehaviorArbiter.gd`, `core/actors/ActorStateMachine.gd`, `core/movement/CombatPressureService.gd`, `core/grid/GridService.gd` (`move_toward` retired), `core/state/flow/states/venture/FlowEncounterState.gd`, `ui/screens/combat/{CombatTokenPresentationState,CombatTokenLayer,CombatBoardScreen}.gd`, `ui/AppRoot.gd` (F1 help string), `data/balance.json`; extended `CombatSnapshotTests`, `CombatTokenPresentationTests`, `CombatRoundtripIntegrationTests`, `BehaviorArbiterTests`, `MovementArbitrationTests`, `ProtectCustodyTests`, `SaveIntegrityTests`, `CombatPressureTests` | **1309/1309 full suite** (baseline 1281 → 1309), 0 failed; compile clean. **6B/6C** cut movement over to live for combat and stage. **6D** ships the minimal presentation stopgap: `MovementResult.actual_traversed_cells` is threaded VERBATIM into `snapshot.data.last_actor_action` as additive `path` + `from_pos`, and `CombatTokenPresentationState` walks it per segment instead of lerping straight through walls. Grid→pixel conversion reuses the SAME `_board.map_to_local` + `_draw_pos` calls that build every token, so a waypoint is pixel-identical by construction; the state stays a pure `RefCounted`. **Total-duration clamp holds BY CONSTRUCTION** — `seg_duration = move_duration / seg_count`, so total animation time is invariant to path length and the `$AutoTimer` margins are unchanged (FAST is the binding case: 0.29 s of animation inside a 0.5 s step delay). No busy flag, no new constant, no new timer coupling. **6E — five defects found by activating the selector for the first time.** TWO WERE P1 AND HAD MADE THE MERGED CUTOVER INERT: (1) `_movement_build_direct_option` built a 2-part `option_id` that `MovementOption._validate_option_id` ALWAYS rejected, so every option was dropped, `selection_enabled` was always false, and `BehaviorArbiter.select_movement_intent` never ran in live combat — every actor silently used legacy nearest-enemy `select_intent`; now reuses the canonical `MovementOptionService._option_id` rather than duplicating the format string. (2) `_crosscheck_perceived_actor` compared `controlling_state` against a raw actor key nobody ever sets (defaulting true) while `FlowRuntime` derives it with the dead/KO/structure conjunction the contract MANDATES (`MovementPerceivedActorFact` → `incapable_actor_cannot_control`); the mismatch discarded the ENTIRE board's selection. Census before/after: `purify_shrine`/`recover`/`protect`/`guide_spirit` were 100% arbiter-rejected → 0%. Also fixed: `duplicate_goal_id` (ids carry no target discriminator; two goals about two hostiles whose adjacency regions share an anchor collide, costing the mover ALL THREE goals — fixed producer-side, id format untouched because it is contract-load-bearing; endure 6.84% → 0%), selection gated on options rather than goals (4–13% of activations fell back to legacy; the PURSUE quarry is excluded because its empty options are DELIBERATE — `FleeBehaviorModule` owns its movement, and gating on goals alone stopped it fleeing), a destroyed objective still published as alive, order-dependent occupancy under stacking, an unguarded `commitment/capacity` divide (NaN → `non_finite_candidate_score` → whole board discarded), and a PURSUE spawn-geometry bug (quarry row tie-break anchored to the BOARD midpoint, not the party's spawn band — a Chebyshev gap `_op_f_p3` never governed, making the mode losable by geometry alone on tall boards). **Truthful handoff to V2-COMBAT-003:** `hostile_control_sources` and `hazard_summary` were hardcoded empty and are now published for real via the same helpers `MovementOptionService` uses. **Test gaps closed:** F2 custody branches (`mover_downed`, `carrier_downed`, invalidated pickup), F3 legacy origin-included path × repaired origin, F4 `_003_repaired` deliberately unset for presentation-only keys (PINNED, not fixed), E1 truncated range-bound objective option, E2 purify with empty `target_id`. Every P1 fix is guarded by a test PROVEN to fail against the pre-fix code (revert-and-watch-it-fail). **Explicitly deferred to `V2-COMBAT-003` (Movement Model Slice C), per this story's ownership clause ("does not own the final behavior arbitration order"):** the `exposure`/`cohesion`/`congestion` option fields stay `0.0` — they are WEIGHTED terms in `_spatial_utility`, so populating them activates scoring weights that have never run in a live encounter, and filling them is inseparable from tuning them; purify positioning above 50% shrine health, which cannot be published without reweighting arbitration (a scoped attempt flipped 3 of 20 seeded encounters from win to `shrine_destroyed` and was reverted); the `engage`-region-excludes-origin question; and unifying the fragile `health_ratio` derivation ladder. **Hazard damages remain the PROPOSED DEFAULTS (unstable `fallback_damage` 3, burning `end_activation_damage` 3) — still awaiting Jeff's explicit ratification; the `_comment` marker in `data/balance.json` is deliberately left in place until then.** |

---

## Foundation Wave — Continuity Systems

| Story | Wave | Status | Ships | Notes |
|-------|------|--------|-------|-------|
| **V2-CONTINUITY-001** — Continuity Flame + Institution Gating | Foundation | Done | `ContinuityService.gd`, `ContinuityFlameControl.gd`, `FlowSanctumState.gd` (continuity_points + continuity_band in snapshot data), `InstitutionService.gd` (blocker_reason), `SaveSchema.gd` (sanctum.rejection_counts), `balance.json` (data.continuity.bands), `docs/continuity-visual-design.md` | Household Fire visual system — 6 bands (Awakening → Cultural Maturity), ember-toned TitleRow indicator distinct from Ase Flame. ContinuityService is single choke for all mutations. Thread accept +5, thread reject escalating (capped), vow break −3. Institution virtue gating via blocker_reason. Band transition ceremonial moment deferred to V2-CONTINUITY-003. Visual design record: `docs/continuity-visual-design.md`. |

---

## Current Foundation Work

| Story | Wave | State | Notes |
|-------|------|-------|-------|
| **V2-COMBAT-002** | Foundation | In Progress | Slices 1–5 accepted and merged to `main` (PRs #45 / #46 / #47 / #48 / #49); Slice 6A (cutover contract + config prep) landed dormant (PR #51). **Slices 6B+6C (this branch) are now LIVE — the shared movement layer is cut over from dormant to live for combat and stage.** Actor movement resolves through `CombatActivationService` / `MovementExecutor`; the arbiter's movement-aware selection and party-based stage traversal are live; `GridService.move_toward` is retired. **Bug fix (movement freeze):** the live planner now models the hostile-control surcharge the executor charges — new seam `MovementOptionService.hostile_edge_costs` threaded into all live planner calls, and legacy commitment uses the surcharged route cost — so actors no longer freeze one tile short of contact in every mode. **Bug fix (top-left convergence):** the combat destination tie-break now uses the same salted FNV-1a de-aligner as the 6A stage frontier selection (new seam `StagePartyMovementAdapter.salted_cell_hash`, keyed by mover), removing the systematic compass bias. Suite at 1281/1281. **Remaining scope (as planned):** fuller docs (`CONVENTIONS.md`, `docs/movement-model.md` amendments, `MEMORY.md`) and the minimal presentation stopgap → Slice 6D/6E. A unified action-point economy was proposed and deferred to its own story (`docs/proposals/action-point-economy.md`, untracked). V2-DIRECTIVE-001 is already shipped (a stale "Next up" marker persists in the user-owned `AGENTS.md` — left untouched). |

---

## Architecture Reference (quick links)

- Snapshot contract → `CONVENTIONS.md` § Snapshot Shape
- Action types → `CONVENTIONS.md` § Action Type Registry
- Save schema → `core/save/SaveSchema.gd`
- Balance config → `data/balance.json`
- Flow states → `core/state/flow/states/`
- Test runner → `ui/AppRoot.gd` → `_run_tests()`
